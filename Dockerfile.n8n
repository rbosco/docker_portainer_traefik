FROM docker.n8n.io/n8nio/n8n:latest

USER root

# DOCX text extraction dependencies for n8n workflows.
RUN npm install -g mammoth && \
    apt-get update && \
    apt-get install -y --no-install-recommends pandoc && \
    apt-get clean && \
    rm -rf /var/lib/apt/lists/*

USER node
